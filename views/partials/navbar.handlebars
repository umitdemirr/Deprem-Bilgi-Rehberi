<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <i class="fas fa-house-damage"></i>
            Deprem Rehberi
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{#if (eq active 'home')}}active{{/if}}" href="/">
                        <i class="fas fa-home"></i> Anasayfa
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq active 'earthquakes')}}active{{/if}}" href="/son-depremler">
                        <i class="fas fa-chart-line"></i> Son Depremler
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq active 'guvenli-bolge')}}active{{/if}}" href="/guvenli-bolge">
                        <i class="fas fa-map-marked-alt"></i> Güvenli Bölgeler
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (eq active 'info')}}active{{/if}}" href="/bilgi-kosesi">
                        <i class="fas fa-info-circle"></i> Bilgi Köşesi
                    </a>
                </li>
            </ul>
            <div class="d-flex">
                <button class="btn btn-outline-light" id="theme-toggle">
                    <i class="fas fa-moon"></i>
                </button>
            </div>
        </div>
    </div>
</nav>

<!-- Navbar'ın altındaki boşluk için -->
<div class="navbar-spacer"></div> 